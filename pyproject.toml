[project]
name = "kanoya"
version = "0.1.0"
description = "奈良春日 鹿のや レベニュー・インテリジェンス（需要推定。レートは推定しない）"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
kanoya = "kanoya.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["kanoya"]
